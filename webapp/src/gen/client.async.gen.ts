// Code generated by csilgen. DO NOT EDIT.
// Source: <csil spec>
// Target: typescript-client

import type { AddGatheringOwnerRequest, AddPeerRequest, AdminList, AdoptGatheringRequest, AttendEventRequest, AttendeeList, BeginLoginRequest, BeginLoginResponse, CreateEventRequest, CreateEventSeriesRequest, CreateGatheringRequest, CreateGreetingRequest, CreateOrganizationRequest, CreateWebhookRequest, DeleteEventRequest, DeleteEventSeriesRequest, DeleteGatheringRequest, DeleteOrganizationRequest, DeleteWebhookRequest, DeliveryReceipt, DevLoginRequest, Empty, Event, EventList, EventRoleList, EventSeries, EventSeriesList, ExpandEventSeriesRequest, FederationIdentity, FindUserRequest, Gathering, GatheringList, GatheringMemberList, GatheringOffer, GatheringOfferList, GetEventRequest, GetEventSeriesRequest, GetGatheringRequest, GetGreetingRequest, GetOrganizationRequest, Greeting, GreetingList, InstanceSettings, JoinGatheringRequest, LeaveGatheringRequest, ListAttendeesRequest, ListEventRolesRequest, ListEventSeriesRequest, ListEventsRequest, ListGatheringMembersRequest, ListGatheringOffersRequest, ListGatheringsRequest, ListOrganizationMembersRequest, ListOrganizationsRequest, ListOriginVolumeRequest, ListPeersRequest, ListRemoteEventsRequest, ListWebhooksRequest, OfferGatheringRequest, Organization, OrganizationList, OrganizationMemberList, OriginVolume, OriginVolumeList, Peer, PeerList, PeeringReceipt, RemoteEventList, RemoveEventRoleRequest, RemoveGatheringOwnerRequest, RemoveOrganizationMemberRequest, RemovePeerRequest, RespondToGatheringOfferRequest, ResumePeerRequest, SearchRequest, SearchResults, SearchUsersRequest, SetAdminRequest, SetEventRoleRequest, SetOrganizationMemberRequest, SetOriginRateLimitRequest, SetPeerRateLimitRequest, SetPeerStatusRequest, SignedDelivery, SignedPeeringRequest, UnattendEventRequest, UpdateEventRequest, UpdateEventSeriesRequest, UpdateGatheringRequest, UpdateInstanceSettingsRequest, UpdateOrganizationRequest, UpdateWebhookRequest, UserProfile, UserRef, UserRefList, Webhook, WebhookList, WebhookWithSecret, WithdrawGatheringOfferRequest } from "./types.gen.ts";
import { fromAdminListCbor, fromAttendeeListCbor, fromBeginLoginResponseCbor, fromDeliveryReceiptCbor, fromEmptyCbor, fromEventCbor, fromEventListCbor, fromEventRoleListCbor, fromEventSeriesCbor, fromEventSeriesListCbor, fromFederationIdentityCbor, fromGatheringCbor, fromGatheringListCbor, fromGatheringMemberListCbor, fromGatheringOfferCbor, fromGatheringOfferListCbor, fromGreetingCbor, fromGreetingListCbor, fromInstanceSettingsCbor, fromOrganizationCbor, fromOrganizationListCbor, fromOrganizationMemberListCbor, fromOriginVolumeCbor, fromOriginVolumeListCbor, fromPeerCbor, fromPeerListCbor, fromPeeringReceiptCbor, fromRemoteEventListCbor, fromSearchResultsCbor, fromUserProfileCbor, fromUserRefCbor, fromUserRefListCbor, fromWebhookCbor, fromWebhookListCbor, fromWebhookWithSecretCbor, toAddGatheringOwnerRequestCbor, toAddPeerRequestCbor, toAdoptGatheringRequestCbor, toAttendEventRequestCbor, toBeginLoginRequestCbor, toCreateEventRequestCbor, toCreateEventSeriesRequestCbor, toCreateGatheringRequestCbor, toCreateGreetingRequestCbor, toCreateOrganizationRequestCbor, toCreateWebhookRequestCbor, toDeleteEventRequestCbor, toDeleteEventSeriesRequestCbor, toDeleteGatheringRequestCbor, toDeleteOrganizationRequestCbor, toDeleteWebhookRequestCbor, toDevLoginRequestCbor, toEmptyCbor, toExpandEventSeriesRequestCbor, toFindUserRequestCbor, toGetEventRequestCbor, toGetEventSeriesRequestCbor, toGetGatheringRequestCbor, toGetGreetingRequestCbor, toGetOrganizationRequestCbor, toJoinGatheringRequestCbor, toLeaveGatheringRequestCbor, toListAttendeesRequestCbor, toListEventRolesRequestCbor, toListEventSeriesRequestCbor, toListEventsRequestCbor, toListGatheringMembersRequestCbor, toListGatheringOffersRequestCbor, toListGatheringsRequestCbor, toListOrganizationMembersRequestCbor, toListOrganizationsRequestCbor, toListOriginVolumeRequestCbor, toListPeersRequestCbor, toListRemoteEventsRequestCbor, toListWebhooksRequestCbor, toOfferGatheringRequestCbor, toRemoveEventRoleRequestCbor, toRemoveGatheringOwnerRequestCbor, toRemoveOrganizationMemberRequestCbor, toRemovePeerRequestCbor, toRespondToGatheringOfferRequestCbor, toResumePeerRequestCbor, toSearchRequestCbor, toSearchUsersRequestCbor, toSetAdminRequestCbor, toSetEventRoleRequestCbor, toSetOrganizationMemberRequestCbor, toSetOriginRateLimitRequestCbor, toSetPeerRateLimitRequestCbor, toSetPeerStatusRequestCbor, toSignedDeliveryCbor, toSignedPeeringRequestCbor, toUnattendEventRequestCbor, toUpdateEventRequestCbor, toUpdateEventSeriesRequestCbor, toUpdateGatheringRequestCbor, toUpdateInstanceSettingsRequestCbor, toUpdateOrganizationRequestCbor, toUpdateWebhookRequestCbor, toWithdrawGatheringOfferRequestCbor } from "./codec.gen.ts";

export interface AsyncServiceTransport {
  call(service: string, op: string, req: Uint8Array): Promise<Uint8Array>;
}

export class AdminAsyncClient {
  constructor(private readonly t: AsyncServiceTransport) {}

  /**
   * Everybody holding the admin role. Admins only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listAdmins(req: Empty): Promise<AdminList> {
    const csilResp = await this.t.call("AdminService", "list-admins", toEmptyCbor(req));
    return fromAdminListCbor(csilResp);
  }

  /**
   * Grant or revoke the role. Admins only. The last admin cannot be
   * revoked.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async setAdmin(req: SetAdminRequest): Promise<AdminList> {
    const csilResp = await this.t.call("AdminService", "set-admin", toSetAdminRequestCbor(req));
    return fromAdminListCbor(csilResp);
  }

  /**
   * Resolve one federated address to a user. Requires a session.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async findUser(req: FindUserRequest): Promise<UserRef> {
    const csilResp = await this.t.call("AdminService", "find-user", toFindUserRequestCbor(req));
    return fromUserRefCbor(csilResp);
  }

  /**
   * Complete a handle prefix. Requires a session.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async searchUsers(req: SearchUsersRequest): Promise<UserRefList> {
    const csilResp = await this.t.call("AdminService", "search-users", toSearchUsersRequestCbor(req));
    return fromUserRefListCbor(csilResp);
  }

  /**
   * The settings an administrator can change while the service runs.
   * Admins only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async getInstanceSettings(req: Empty): Promise<InstanceSettings> {
    const csilResp = await this.t.call("AdminService", "get-instance-settings", toEmptyCbor(req));
    return fromInstanceSettingsCbor(csilResp);
  }

  /**
   * Change them. An absent field is left alone. Admins only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async updateInstanceSettings(req: UpdateInstanceSettingsRequest): Promise<InstanceSettings> {
    const csilResp = await this.t.call("AdminService", "update-instance-settings", toUpdateInstanceSettingsRequestCbor(req));
    return fromInstanceSettingsCbor(csilResp);
  }
}

export class AuthAsyncClient {
  constructor(private readonly t: AsyncServiceTransport) {}

  /**
   * Start a login. The api signs a self-verifying nonce and returns the
   * identity-provider URL for the browser to open.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async beginLogin(req: BeginLoginRequest): Promise<BeginLoginResponse> {
    const csilResp = await this.t.call("AuthService", "begin-login", toBeginLoginRequestCbor(req));
    return fromBeginLoginResponseCbor(csilResp);
  }

  /**
   * Clear the caller's session cookie. Idempotent; never fails, even when
   * the caller was already logged out.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async logout(req: Empty): Promise<Empty> {
    const csilResp = await this.t.call("AuthService", "logout", toEmptyCbor(req));
    return fromEmptyCbor(csilResp);
  }

  /**
   * The caller's own profile, derived from the session cookie.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async whoami(req: Empty): Promise<UserProfile> {
    const csilResp = await this.t.call("AuthService", "whoami", toEmptyCbor(req));
    return fromUserProfileCbor(csilResp);
  }
}

export class DevAuthAsyncClient {
  constructor(private readonly t: AsyncServiceTransport) {}

  /**
   * Mint a session for `handle@domain`, creating the user on first use.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async devLogin(req: DevLoginRequest): Promise<UserProfile> {
    const csilResp = await this.t.call("DevAuthService", "dev-login", toDevLoginRequestCbor(req));
    return fromUserProfileCbor(csilResp);
  }
}

export class EventAsyncClient {
  constructor(private readonly t: AsyncServiceTransport) {}

  /**
   * Events matching a time window. Anonymous callers are allowed.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listEvents(req: ListEventsRequest): Promise<EventList> {
    const csilResp = await this.t.call("EventService", "list-events", toListEventsRequestCbor(req));
    return fromEventListCbor(csilResp);
  }

  /**
   * One event by id. Anonymous callers are allowed. `description` is
   * absent once the event has started.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async getEvent(req: GetEventRequest): Promise<Event> {
    const csilResp = await this.t.call("EventService", "get-event", toGetEventRequestCbor(req));
    return fromEventCbor(csilResp);
  }

  /**
   * Schedule a single event. Gathering owners only — organizer is a
   * role held on an event that already exists, so it cannot be what
   * authorizes making one.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async createEvent(req: CreateEventRequest): Promise<Event> {
    const csilResp = await this.t.call("EventService", "create-event", toCreateEventRequestCbor(req));
    return fromEventCbor(csilResp);
  }

  /**
   * Change an event. Refused once it has started.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async updateEvent(req: UpdateEventRequest): Promise<Event> {
    const csilResp = await this.t.call("EventService", "update-event", toUpdateEventRequestCbor(req));
    return fromEventCbor(csilResp);
  }

  /**
   * Destroy an event. Owners and organizers before it starts; admins
   * only afterwards.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async deleteEvent(req: DeleteEventRequest): Promise<Empty> {
    const csilResp = await this.t.call("EventService", "delete-event", toDeleteEventRequestCbor(req));
    return fromEmptyCbor(csilResp);
  }

  /**
   * Mark the caller attending. Needs gathering membership; refused once
   * the event has started.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async attendEvent(req: AttendEventRequest): Promise<Event> {
    const csilResp = await this.t.call("EventService", "attend-event", toAttendEventRequestCbor(req));
    return fromEventCbor(csilResp);
  }

  /**
   * Withdraw the caller's attendance. Refused once the event has started.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async unattendEvent(req: UnattendEventRequest): Promise<Event> {
    const csilResp = await this.t.call("EventService", "unattend-event", toUnattendEventRequestCbor(req));
    return fromEventCbor(csilResp);
  }

  /**
   * Who is attending. Anonymous callers are allowed.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listAttendees(req: ListAttendeesRequest): Promise<AttendeeList> {
    const csilResp = await this.t.call("EventService", "list-attendees", toListAttendeesRequestCbor(req));
    return fromAttendeeListCbor(csilResp);
  }

  /**
   * Every series under a gathering. Anonymous callers are allowed.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listEventSeries(req: ListEventSeriesRequest): Promise<EventSeriesList> {
    const csilResp = await this.t.call("EventService", "list-event-series", toListEventSeriesRequestCbor(req));
    return fromEventSeriesListCbor(csilResp);
  }

  /**
   * One series by id. Anonymous callers are allowed.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async getEventSeries(req: GetEventSeriesRequest): Promise<EventSeries> {
    const csilResp = await this.t.call("EventService", "get-event-series", toGetEventSeriesRequestCbor(req));
    return fromEventSeriesCbor(csilResp);
  }

  /**
   * Schedule a recurring set. Gathering owners only, for the same
   * reason create-event is.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async createEventSeries(req: CreateEventSeriesRequest): Promise<EventSeries> {
    const csilResp = await this.t.call("EventService", "create-event-series", toCreateEventSeriesRequestCbor(req));
    return fromEventSeriesCbor(csilResp);
  }

  /**
   * Change a series, rewriting the occurrences that have not started.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async updateEventSeries(req: UpdateEventSeriesRequest): Promise<EventSeries> {
    const csilResp = await this.t.call("EventService", "update-event-series", toUpdateEventSeriesRequestCbor(req));
    return fromEventSeriesCbor(csilResp);
  }

  /**
   * Destroy a series and its occurrences that have not started.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async deleteEventSeries(req: DeleteEventSeriesRequest): Promise<Empty> {
    const csilResp = await this.t.call("EventService", "delete-event-series", toDeleteEventSeriesRequestCbor(req));
    return fromEmptyCbor(csilResp);
  }

  /**
   * Materialize further occurrences and return them all. Idempotent.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async expandEventSeries(req: ExpandEventSeriesRequest): Promise<EventList> {
    const csilResp = await this.t.call("EventService", "expand-event-series", toExpandEventSeriesRequestCbor(req));
    return fromEventListCbor(csilResp);
  }

  /**
   * Give somebody organizer or presenter on an event or a series.
   * Gathering owners only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async setEventRole(req: SetEventRoleRequest): Promise<EventRoleList> {
    const csilResp = await this.t.call("EventService", "set-event-role", toSetEventRoleRequestCbor(req));
    return fromEventRoleListCbor(csilResp);
  }

  /**
   * Take a role away. Gathering owners only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async removeEventRole(req: RemoveEventRoleRequest): Promise<EventRoleList> {
    const csilResp = await this.t.call("EventService", "remove-event-role", toRemoveEventRoleRequestCbor(req));
    return fromEventRoleListCbor(csilResp);
  }

  /**
   * Who holds roles on an event or a series. Anonymous callers allowed.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listEventRoles(req: ListEventRolesRequest): Promise<EventRoleList> {
    const csilResp = await this.t.call("EventService", "list-event-roles", toListEventRolesRequestCbor(req));
    return fromEventRoleListCbor(csilResp);
  }
}

export class FederationAsyncClient {
  constructor(private readonly t: AsyncServiceTransport) {}

  /**
   * What this instance is, as a peer would address it. Anonymous callers
   * are allowed: an address is a name, not a secret.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async federationIdentity(req: Empty): Promise<FederationIdentity> {
    const csilResp = await this.t.call("FederationService", "federation-identity", toEmptyCbor(req));
    return fromFederationIdentityCbor(csilResp);
  }

  /**
   * Every peer and its standing in both directions. Admins only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listPeers(req: ListPeersRequest): Promise<PeerList> {
    const csilResp = await this.t.call("FederationService", "list-peers", toListPeersRequestCbor(req));
    return fromPeerListCbor(csilResp);
  }

  /**
   * Register a directory to publish to, and ask it to accept us.
   * Admins only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async addPeer(req: AddPeerRequest): Promise<Peer> {
    const csilResp = await this.t.call("FederationService", "add-peer", toAddPeerRequestCbor(req));
    return fromPeerCbor(csilResp);
  }

  /**
   * Answer a peer's request, or change our mind. Admins only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async setPeerStatus(req: SetPeerStatusRequest): Promise<Peer> {
    const csilResp = await this.t.call("FederationService", "set-peer-status", toSetPeerStatusRequestCbor(req));
    return fromPeerCbor(csilResp);
  }

  /**
   * Restart delivery to a suspended peer. Admins only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async resumePeer(req: ResumePeerRequest): Promise<Peer> {
    const csilResp = await this.t.call("FederationService", "resume-peer", toResumePeerRequestCbor(req));
    return fromPeerCbor(csilResp);
  }

  /**
   * Forget a peer and everything it sent. Admins only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async removePeer(req: RemovePeerRequest): Promise<Empty> {
    const csilResp = await this.t.call("FederationService", "remove-peer", toRemovePeerRequestCbor(req));
    return fromEmptyCbor(csilResp);
  }

  /**
   * Accept a signed batch from a peer. Called by an instance, not a
   * person. Refused unless the peer's inbound status is approved.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async deliverEvents(req: SignedDelivery): Promise<DeliveryReceipt> {
    const csilResp = await this.t.call("FederationService", "deliver-events", toSignedDeliveryCbor(req));
    return fromDeliveryReceiptCbor(csilResp);
  }

  /**
   * Ask to be accepted as a peer. Called by an instance, not a person.
   * Recorded as pending; an administrator here decides.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async requestPeering(req: SignedPeeringRequest): Promise<PeeringReceipt> {
    const csilResp = await this.t.call("FederationService", "request-peering", toSignedPeeringRequestCbor(req));
    return fromPeeringReceiptCbor(csilResp);
  }

  /**
   * What peers have sent. Anonymous callers are allowed — a directory
   * exists to be read.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listRemoteEvents(req: ListRemoteEventsRequest): Promise<RemoteEventList> {
    const csilResp = await this.t.call("FederationService", "list-remote-events", toListRemoteEventsRequestCbor(req));
    return fromRemoteEventListCbor(csilResp);
  }

  /**
   * Give one peer its own events-per-minute allowance, or restore the
   * instance-wide one. Admins only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async setPeerRateLimit(req: SetPeerRateLimitRequest): Promise<Peer> {
    const csilResp = await this.t.call("FederationService", "set-peer-rate-limit", toSetPeerRateLimitRequestCbor(req));
    return fromPeerCbor(csilResp);
  }

  /**
   * How much each originating organization is sending, busiest first.
   * Admins only — it is operational detail, not a public listing.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listOriginVolume(req: ListOriginVolumeRequest): Promise<OriginVolumeList> {
    const csilResp = await this.t.call("FederationService", "list-origin-volume", toListOriginVolumeRequestCbor(req));
    return fromOriginVolumeListCbor(csilResp);
  }

  /**
   * Give one originating organization its own events-per-minute
   * allowance, or restore the instance-wide one. Admins only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async setOriginRateLimit(req: SetOriginRateLimitRequest): Promise<OriginVolume> {
    const csilResp = await this.t.call("FederationService", "set-origin-rate-limit", toSetOriginRateLimitRequestCbor(req));
    return fromOriginVolumeCbor(csilResp);
  }
}

export class GatheringAsyncClient {
  constructor(private readonly t: AsyncServiceTransport) {}

  /**
   * Every gathering, or only the caller's with `mine`. Anonymous
   * callers are allowed.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listGatherings(req: ListGatheringsRequest): Promise<GatheringList> {
    const csilResp = await this.t.call("GatheringService", "list-gatherings", toListGatheringsRequestCbor(req));
    return fromGatheringListCbor(csilResp);
  }

  /**
   * One gathering by id. Anonymous callers are allowed.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async getGathering(req: GetGatheringRequest): Promise<Gathering> {
    const csilResp = await this.t.call("GatheringService", "get-gathering", toGetGatheringRequestCbor(req));
    return fromGatheringCbor(csilResp);
  }

  /**
   * Start a gathering, owned by the caller or by an organization the
   * caller owns.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async createGathering(req: CreateGatheringRequest): Promise<Gathering> {
    const csilResp = await this.t.call("GatheringService", "create-gathering", toCreateGatheringRequestCbor(req));
    return fromGatheringCbor(csilResp);
  }

  /**
   * Change a gathering's own fields. Owners only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async updateGathering(req: UpdateGatheringRequest): Promise<Gathering> {
    const csilResp = await this.t.call("GatheringService", "update-gathering", toUpdateGatheringRequestCbor(req));
    return fromGatheringCbor(csilResp);
  }

  /**
   * Destroy a gathering and every event under it. Owners or an admin.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async deleteGathering(req: DeleteGatheringRequest): Promise<Empty> {
    const csilResp = await this.t.call("GatheringService", "delete-gathering", toDeleteGatheringRequestCbor(req));
    return fromEmptyCbor(csilResp);
  }

  /**
   * A gathering's roster. Anonymous callers are allowed.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listGatheringMembers(req: ListGatheringMembersRequest): Promise<GatheringMemberList> {
    const csilResp = await this.t.call("GatheringService", "list-gathering-members", toListGatheringMembersRequestCbor(req));
    return fromGatheringMemberListCbor(csilResp);
  }

  /**
   * Join. Any authenticated caller. Idempotent.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async joinGathering(req: JoinGatheringRequest): Promise<Gathering> {
    const csilResp = await this.t.call("GatheringService", "join-gathering", toJoinGatheringRequestCbor(req));
    return fromGatheringCbor(csilResp);
  }

  /**
   * Leave. Idempotent.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async leaveGathering(req: LeaveGatheringRequest): Promise<Gathering> {
    const csilResp = await this.t.call("GatheringService", "leave-gathering", toLeaveGatheringRequestCbor(req));
    return fromGatheringCbor(csilResp);
  }

  /**
   * Add an individual or an organization as an owner. Existing owners
   * only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async addGatheringOwner(req: AddGatheringOwnerRequest): Promise<Gathering> {
    const csilResp = await this.t.call("GatheringService", "add-gathering-owner", toAddGatheringOwnerRequestCbor(req));
    return fromGatheringCbor(csilResp);
  }

  /**
   * Drop an owner. The last owner cannot be dropped.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async removeGatheringOwner(req: RemoveGatheringOwnerRequest): Promise<Gathering> {
    const csilResp = await this.t.call("GatheringService", "remove-gathering-owner", toRemoveGatheringOwnerRequestCbor(req));
    return fromGatheringCbor(csilResp);
  }

  /**
   * Offer this gathering to an organization. Gathering owners only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async offerGathering(req: OfferGatheringRequest): Promise<GatheringOffer> {
    const csilResp = await this.t.call("GatheringService", "offer-gathering", toOfferGatheringRequestCbor(req));
    return fromGatheringOfferCbor(csilResp);
  }

  /**
   * Offers the caller can see: made for a gathering they own, or made to
   * an organization they own.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listGatheringOffers(req: ListGatheringOffersRequest): Promise<GatheringOfferList> {
    const csilResp = await this.t.call("GatheringService", "list-gathering-offers", toListGatheringOffersRequestCbor(req));
    return fromGatheringOfferListCbor(csilResp);
  }

  /**
   * Accept or decline an offer. An owner of the receiving organization
   * only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async respondToGatheringOffer(req: RespondToGatheringOfferRequest): Promise<GatheringOffer> {
    const csilResp = await this.t.call("GatheringService", "respond-to-gathering-offer", toRespondToGatheringOfferRequestCbor(req));
    return fromGatheringOfferCbor(csilResp);
  }

  /**
   * Take back a pending offer. The offering side only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async withdrawGatheringOffer(req: WithdrawGatheringOfferRequest): Promise<Empty> {
    const csilResp = await this.t.call("GatheringService", "withdraw-gathering-offer", toWithdrawGatheringOfferRequestCbor(req));
    return fromEmptyCbor(csilResp);
  }

  /**
   * Put a loose gathering under an organization. Administrators only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async adoptGathering(req: AdoptGatheringRequest): Promise<Gathering> {
    const csilResp = await this.t.call("GatheringService", "adopt-gathering", toAdoptGatheringRequestCbor(req));
    return fromGatheringCbor(csilResp);
  }
}

export class GreetingAsyncClient {
  constructor(private readonly t: AsyncServiceTransport) {}

  /**
   * Every greeting, newest first. Anonymous callers are allowed.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listGreetings(req: Empty): Promise<GreetingList> {
    const csilResp = await this.t.call("GreetingService", "list-greetings", toEmptyCbor(req));
    return fromGreetingListCbor(csilResp);
  }

  /**
   * One greeting by id. Anonymous callers are allowed.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async getGreeting(req: GetGreetingRequest): Promise<Greeting> {
    const csilResp = await this.t.call("GreetingService", "get-greeting", toGetGreetingRequestCbor(req));
    return fromGreetingCbor(csilResp);
  }

  /**
   * Leave a greeting. Requires a session.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async createGreeting(req: CreateGreetingRequest): Promise<Greeting> {
    const csilResp = await this.t.call("GreetingService", "create-greeting", toCreateGreetingRequestCbor(req));
    return fromGreetingCbor(csilResp);
  }
}

export class OrganizationAsyncClient {
  constructor(private readonly t: AsyncServiceTransport) {}

  /**
   * Every organization, newest first, or only the caller's with `mine`.
   * Anonymous callers are allowed.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listOrganizations(req: ListOrganizationsRequest): Promise<OrganizationList> {
    const csilResp = await this.t.call("OrganizationService", "list-organizations", toListOrganizationsRequestCbor(req));
    return fromOrganizationListCbor(csilResp);
  }

  /**
   * One organization by id. Anonymous callers are allowed.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async getOrganization(req: GetOrganizationRequest): Promise<Organization> {
    const csilResp = await this.t.call("OrganizationService", "get-organization", toGetOrganizationRequestCbor(req));
    return fromOrganizationCbor(csilResp);
  }

  /**
   * Start an organization. The caller becomes its first owner.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async createOrganization(req: CreateOrganizationRequest): Promise<Organization> {
    const csilResp = await this.t.call("OrganizationService", "create-organization", toCreateOrganizationRequestCbor(req));
    return fromOrganizationCbor(csilResp);
  }

  /**
   * Change an organization's own fields. Owners only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async updateOrganization(req: UpdateOrganizationRequest): Promise<Organization> {
    const csilResp = await this.t.call("OrganizationService", "update-organization", toUpdateOrganizationRequestCbor(req));
    return fromOrganizationCbor(csilResp);
  }

  /**
   * Destroy an organization. Admins only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async deleteOrganization(req: DeleteOrganizationRequest): Promise<Empty> {
    const csilResp = await this.t.call("OrganizationService", "delete-organization", toDeleteOrganizationRequestCbor(req));
    return fromEmptyCbor(csilResp);
  }

  /**
   * An organization's roster. Anonymous callers are allowed.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listOrganizationMembers(req: ListOrganizationMembersRequest): Promise<OrganizationMemberList> {
    const csilResp = await this.t.call("OrganizationService", "list-organization-members", toListOrganizationMembersRequestCbor(req));
    return fromOrganizationMemberListCbor(csilResp);
  }

  /**
   * Add somebody, or change their standing. Owners only.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async setOrganizationMember(req: SetOrganizationMemberRequest): Promise<OrganizationMemberList> {
    const csilResp = await this.t.call("OrganizationService", "set-organization-member", toSetOrganizationMemberRequestCbor(req));
    return fromOrganizationMemberListCbor(csilResp);
  }

  /**
   * Take somebody off the roster. Owners, or the person themselves.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async removeOrganizationMember(req: RemoveOrganizationMemberRequest): Promise<OrganizationMemberList> {
    const csilResp = await this.t.call("OrganizationService", "remove-organization-member", toRemoveOrganizationMemberRequestCbor(req));
    return fromOrganizationMemberListCbor(csilResp);
  }
}

export class SearchAsyncClient {
  constructor(private readonly t: AsyncServiceTransport) {}

  /**
   * Search organizations, gatherings, events and series at once.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async search(req: SearchRequest): Promise<SearchResults> {
    const csilResp = await this.t.call("SearchService", "search", toSearchRequestCbor(req));
    return fromSearchResultsCbor(csilResp);
  }
}

export class WebhookAsyncClient {
  constructor(private readonly t: AsyncServiceTransport) {}

  /**
   * The webhooks on one organization or gathering, with the limit.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listWebhooks(req: ListWebhooksRequest): Promise<WebhookList> {
    const csilResp = await this.t.call("WebhookService", "list-webhooks", toListWebhooksRequestCbor(req));
    return fromWebhookListCbor(csilResp);
  }

  /**
   * Add one. Returns the signing secret, once and only here.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async createWebhook(req: CreateWebhookRequest): Promise<WebhookWithSecret> {
    const csilResp = await this.t.call("WebhookService", "create-webhook", toCreateWebhookRequestCbor(req));
    return fromWebhookWithSecretCbor(csilResp);
  }

  /**
   * Change the URL, the scope, the note, or whether it is switched on.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async updateWebhook(req: UpdateWebhookRequest): Promise<Webhook> {
    const csilResp = await this.t.call("WebhookService", "update-webhook", toUpdateWebhookRequestCbor(req));
    return fromWebhookCbor(csilResp);
  }

  /**
   * Remove one.
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async deleteWebhook(req: DeleteWebhookRequest): Promise<Empty> {
    const csilResp = await this.t.call("WebhookService", "delete-webhook", toDeleteWebhookRequestCbor(req));
    return fromEmptyCbor(csilResp);
  }
}

export class AsyncApiClient {
  readonly admin: AdminAsyncClient;
  readonly auth: AuthAsyncClient;
  readonly devAuth: DevAuthAsyncClient;
  readonly event: EventAsyncClient;
  readonly federation: FederationAsyncClient;
  readonly gathering: GatheringAsyncClient;
  readonly greeting: GreetingAsyncClient;
  readonly organization: OrganizationAsyncClient;
  readonly search: SearchAsyncClient;
  readonly webhook: WebhookAsyncClient;
  constructor(t: AsyncServiceTransport) {
    this.admin = new AdminAsyncClient(t);
    this.auth = new AuthAsyncClient(t);
    this.devAuth = new DevAuthAsyncClient(t);
    this.event = new EventAsyncClient(t);
    this.federation = new FederationAsyncClient(t);
    this.gathering = new GatheringAsyncClient(t);
    this.greeting = new GreetingAsyncClient(t);
    this.organization = new OrganizationAsyncClient(t);
    this.search = new SearchAsyncClient(t);
    this.webhook = new WebhookAsyncClient(t);
  }
}
