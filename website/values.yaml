# For the catalyst-helm/pysocha-site helm chart.
# See https://github.com/catalystcommunity/chart-pysocha-site

domain: tinkucommunities.com

sitefilePath: "/site/root"
image:
  # The reactorcide deploy job sets both again with --set. They are here so
  # that a person can run the same helm command by hand.
  repository: containers.catalystsquad.com/public/catalystcommunity/tinku-website
  # tag comes from the release tag, which the deploy job passes with --set
