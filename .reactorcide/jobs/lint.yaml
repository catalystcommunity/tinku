name: tinku-lint
description: "gofmt, go vet, and the web client typecheck."
job:
  image: containers.catalystsquad.com/public/reactorcide/runnerbase:latest
  command: "runnerlib run --job-command true"
  timeout: 900
  priority: 10
  working_dir: /job
environment:
  TINKU_JOB: lint
