name: tinku-website
description: "Build the marketing site and check the committed output matches."
job:
  image: containers.catalystsquad.com/public/reactorcide/runnerbase:latest
  command: "runnerlib run --job-command true"
  timeout: 900
  priority: 10
  working_dir: /job
environment:
  TINKU_JOB: website
